@page
@model HairdresserAppointmentClient.Pages.LoginModel
@{
}


<div class="vh-100" style="margin-top: -40px;">

	<img src="~/images/loginbild2.png" class="position-fixed top-0 start-0 w-100 h-100 object-fit-cover d-block opacity-75"/> 

	<div class="position-fixed top-50 start-50 translate-middle p-4 rounded text-center text-white"
		 style="
		backdrop-filter: blur(20px);
		background: rgba(0,0,0,0.4);
		border-radius: 25px;
		padding: 40px;
		width: 380px;
		">


		<form method="post">
			<div class="mb-3 text-start">
				<label class="form-label">Username</label>
				<input asp-for="@Model.loginDto.Email" class="form-control" />
			</div>
			<div class="mb-3 text-start">
				<label class="form-label">Password</label>
				<input asp-for="@Model.loginDto.Password" type="password" class="form-control" />
			</div>
			<div class="mt-2">
				@if (Model.ErrorMessage != null)
				{
					<div class="alert alert-danger py-1 px-2 small">
						@Model.ErrorMessage
					</div>
				}
			</div>
			<button type="submit" class="btn btn-outline-light w-50 fw-bold">Login</button>
		</form>
		
	</div>

	


</div>
